<%= form_for @user do |f| %>
  <%= render partial: 'partials/user_form_template', locals: {f: f, value: nil} %>
  <%= render partial: 'partials/address_form_template', locals: {f: f} %>
  <%= render partial: 'partials/user_password_template', locals: {f: f} %>
  <p><%= f.submit %></p>
<%end %>
