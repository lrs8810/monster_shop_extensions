<%= form_for current_user, url: profile_edit_path(current_user) do |f| %>
  <%=
    render partial: 'partials/user_form_template', locals: {
      name: current_user.name,
      email: current_user.email,
      f: f
    }
  %>
  <p><%= f.submit %></p>
<% end %>
